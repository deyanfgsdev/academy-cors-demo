{
  "name": "cors",
  "version": "1.0.0",
  "description": "demo example about CORS",
  "main": "src/server/server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node src/server/server.js",
    "live": "nodemon src/server/server.js"
  },
  "keywords": [
    "cors"
  ],
  "author": "deyan && oscar",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.19"
  }
}